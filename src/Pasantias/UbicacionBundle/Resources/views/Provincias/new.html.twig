{% extends 'ThemeBundle:Default:base.html.twig' %}
{% block contenido %}
<h1>Alta Provincias</h1>
<div class="well bs-component col-lg-10">
    <form action="{{ path('pasantias_provincias_create') }}" method="post" {{ form_enctype(form) }}>
        <fieldset>
{{ form_widget(form) }}
        </fieldset>   
        <div class="record_actions pull-right">
            <button class="btn btn-primary"type="submit"><i class="glyphicon glyphicon-floppy-disk"></i> Guardar</button>
            <a class="btn btn-default" href="{{ path('pasantias_provincias') }}">
                Volver
            </a>

        </div>
    </form>
</div>
{%endblock%}