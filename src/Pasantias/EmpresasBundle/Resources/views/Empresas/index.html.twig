{% extends 'ThemeBundle:Default:base.html.twig' %}
{% block head%}

{% endblock %}

{% block contenido %}
<h1>Empresas list</h1>

<table class="table table-striped table-hover ">
    <thead>
        <tr>
            <th>Nro</th>
            <th>Nombre</th>
            <th>Persona contacto</th>
            <th>Tel</th>
            <th>Mail</th>
            {#<th>Paginaweb</th>#}
            <th>Actions</th>
        </tr>
    </thead>
    <tbody>
    {% for entity in entities %}
        <tr>
                    {#<td><a href="{{ path('empresas_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>#}
            <td>{{ loop.index }}</td>
            <td>{{ entity.nombreEmpresa }}</td>
            <td>{{ entity.personaContacto }}</td>
            <td>{{ entity.telContacto }}</td>
            <td>{{ entity.mailContacto }}</td>
                {#<td>{{ entity.paginaWeb }}</td>#}
            <td>
                <ul>
                    <li>
                        <a href="{{ path('empresas_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('empresas_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
            </td>
        </tr>
    {% endfor %}
    </tbody>
</table>



{#<table>
    <tr>
        <th>{{ knp_pagination_sortable(pagination,'Id', 'empresas.id') }}</th>
        <th{% if pagination.isSorted('empresas.nombreEmpresa') %} class="sorted"{% endif %}>
            {{ knp_pagination_sortable(pagination,'Titulo', 'empresas.nombre') }}
            </th>
        </tr>

    {% for entity in pagination %}
        <tr>
            <td>{{ entity.id }}</td>
            <td>{{ entity.nombreEmpresa }}</td>
            <td>{{ entity.personaContacto }}</td>
            <td>{{ entity.telContacto }}</td>
            <td>{{ entity.mailContacto }}</td>
            <td>{{ entity.paginaWeb }}</td>
            <td>
                <ul>
                    <li>
                        <a href="{{ path('empresas_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('empresas_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
            </td>
        </tr>
    {% endfor %}
    </table>

    <div class="navigation">
    {{ knp_pagination_render(pagination) }}
        </div>#}
<br>

<ul>
    <li>
        <a href="{{ path('empresas_new') }}">
            Create a new entry
        </a>
    </li>
</ul>


{%endblock%}
