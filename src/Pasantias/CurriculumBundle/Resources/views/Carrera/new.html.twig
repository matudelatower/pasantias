{% extends 'ThemeBundle:Default:base.html.twig' %}
{% block contenido %}
<h1>Carrera creation</h1>

<form action="{{ path('carrera_create') }}" method="post" {{ form_enctype(form) }}>
    {{ form_widget(form) }}
    <p>
        <button type="submit">Create</button>
    </p>
</form>

<ul class="record_actions">
    <li>
        <a href="{{ path('carrera_homepage') }}">
            Back to the list
        </a>
    </li>
</ul>
{%endblock%}