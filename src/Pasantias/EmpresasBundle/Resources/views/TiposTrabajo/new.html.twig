{% extends 'ThemeBundle:Default:base.html.twig' %}
{% block contenido %}
<h1>TiposTrabajo creation</h1>

<form action="{{ path('tipo_trabajo_create') }}" method="post" {{ form_enctype(form) }}>
    {{ form_widget(form) }}
    <p>
        <button type="submit">Create</button>
    </p>
</form>

<ul class="record_actions">
    <li>
        <a href="{{ path('tipo_trabajo') }}">
            Back to the list
        </a>
    </li>
</ul>
{%endblock%}
