{% extends 'ThemeBundle:Default:base.html.twig' %}
{% block contenido %}
<h1>TipoCarrera creation</h1>

<form action="{{ path('tipo_carrera_create') }}" method="post" {{ form_enctype(form) }}>
    {{ form_widget(form) }}
    <p>
        <button type="submit">Create</button>
    </p>
</form>

<ul class="record_actions">
    <li>
        <a href="{{ path('tipo_carrera_homepage') }}">
            Back to the list
        </a>
    </li>
</ul>
{%endblock%}