{% extends 'ThemeBundle:Default:base.html.twig' %}
{% block contenido %}
<h1>Localidades</h1>

<table class="record_properties">
    <tbody>
        <tr>
            <th>Id</th>
            <td>{{ entity.id }}</td>
        </tr>
        <tr>
            <th>Nomblocalidad</th>
            <td>{{ entity.nombreLocalidad }}</td>
        </tr>
    </tbody>
</table>

<ul class="record_actions">
    <li>
        <a href="{{ path('pasantias_localidades') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('pasantias_localidades_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>
        <form action="{{ path('pasantias_localidades_delete', { 'id': entity.id }) }}" method="post">
            {{ form_widget(delete_form) }}
            <button type="submit">Delete</button>
        </form>
    </li>
</ul>
{%endblock%}