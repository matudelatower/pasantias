<h1>TiposUsuario creation</h1>

<form action="{{ path('pasantias_tiposusuario_create') }}" method="post" {{ form_enctype(form) }}>
    {{ form_widget(form) }}
    <p>
        <button type="submit">Create</button>
    </p>
</form>

<ul class="record_actions">
    <li>
        <a href="{{ path('pasantias_tiposusuario') }}">
            Back to the list
        </a>
    </li>
</ul>
