{% extends 'ThemeBundle:Default:base.html.twig' %}
{% block contenido %}
<h1>Solicitudes creation</h1>

<form action="{{ path('solicitud_create') }}" method="post" {{ form_enctype(form) }}>
    {{ form_widget(form) }}
    <p>
        <button type="submit">Create</button>
    </p>
</form>

<ul class="record_actions">
    <li>
        <a href="{{ path('solicitud') }}">
            Back to the list
        </a>
    </li>
</ul>
{%endblock%}