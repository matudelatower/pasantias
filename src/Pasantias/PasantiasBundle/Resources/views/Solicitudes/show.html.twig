{% extends 'ThemeBundle:Default:base.html.twig' %}
{% block contenido %}
<h1>Solicitudes</h1>

<table class="record_properties">
    <tbody>
        <tr>
            <th>Id</th>
            <td>{{ entity.id }}</td>
        </tr>
        <tr>
            <th>Fecdesde</th>
            <td>{{ entity.fecDesde|date('Y-m-d H:i:s') }}</td>
        </tr>
        <tr>
            <th>Fechasta</th>
            <td>{{ entity.fecHasta|date('Y-m-d H:i:s') }}</td>
        </tr>
        <tr>
            <th>Perfilpostulante</th>
            <td>{{ entity.perfilPostulante }}</td>
        </tr>
        <tr>
            <th>Conocrequeridos</th>
            <td>{{ entity.conocRequeridos }}</td>
        </tr>
        <tr>
            <th>Nivelcarreracandidato</th>
            <td>{{ entity.nivelCarreraCandidato }}</td>
        </tr>
        <tr>
            <th>Soliatendida</th>
            <td>{{ entity.soliAtendida }}</td>
        </tr>
    </tbody>
</table>

<ul class="record_actions">
    <li>
        <a href="{{ path('pasantias_solicitudes') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('pasantias_solicitudes_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>
        <form action="{{ path('pasantias_solicitudes_delete', { 'id': entity.id }) }}" method="post">
            {{ form_widget(delete_form) }}
            <button type="submit">Delete</button>
        </form>
    </li>
</ul>
{%endblock%}