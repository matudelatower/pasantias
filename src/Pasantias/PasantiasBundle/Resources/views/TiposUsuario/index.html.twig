<h1>TiposUsuario list</h1>

<table class="records_list">
    <thead>
        <tr>
            <th>Id</th>
            <th>Nombtipouser</th>
            <th>Actions</th>
        </tr>
    </thead>
    <tbody>
    {% for entity in entities %}
        <tr>
            <td><a href="{{ path('pasantias_tiposusuario_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
            <td>{{ entity.nombTipouser }}</td>
            <td>
                <ul>
                    <li>
                        <a href="{{ path('pasantias_tiposusuario_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('pasantias_tiposusuario_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
            </td>
        </tr>
    {% endfor %}
    </tbody>
</table>

<ul>
    <li>
        <a href="{{ path('pasantias_tiposusuario_new') }}">
            Create a new entry
        </a>
    </li>
</ul>
